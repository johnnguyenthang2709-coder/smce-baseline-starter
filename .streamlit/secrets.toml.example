# Copy to .streamlit/secrets.toml for local secrets (never commit secrets.toml).
# On Streamlit Community Cloud, add the same keys under App settings → Secrets.

# Example — only if your solution needs API keys:
# OPENAI_API_KEY = "sk-..."
